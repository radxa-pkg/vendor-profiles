#!/bin/sh
set -e

action="$1"

if [ "$action" = configure ] && [ -f /etc/apt/preferences.d/librga2 ]; then
	echo "Removing /etc/apt/preferences.d/librga2"
	rm /etc/apt/preferences.d/librga2
fi

#DEBHELPER#

exit 0